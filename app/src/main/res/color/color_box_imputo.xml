<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Color cuando el campo tiene el foco (click) -->
    <item android:color="@color/colorBox" android:state_focused="true" />

    <!-- Color cuando el campo NO tiene el foco (reposo) -->
    <item android:color="@color/colorBox" />


</selector>